[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aegis-context"
version = "0.1.0"
description = "Evidence-backed incident context foundation"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy",
    "alembic",
    "psycopg[binary]",
    "pgvector",
    "pydantic",
    "pydantic-settings",
    "typer",
    "rich",
    "pyyaml",
    "python-dateutil",
    "fastmcp",
    "httpx",
    "fastapi",
    "uvicorn[standard]",
    "openai>=3.3.1",
]

[project.scripts]
aegis = "aegis.cli:app"

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "testcontainers[postgres]",
    "ruff",
    "mypy",
    "freezegun",
]

[tool.hatch.build.targets.wheel]
packages = ["src/aegis"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100
# Agent worktrees live inside the repo and hold another milestone's
# in-progress code; linting them reports failures this tree cannot fix.
exclude = [".claude/worktrees"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.11"
strict = true
